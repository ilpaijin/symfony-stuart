<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>User</title>
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <p>{{users|length}}</p>
        {% for user in users %}
            <p>id #{{user.getId()}}: {{user.getUsernameCanonical()}}</p>
        {% endfor %}
    </body>
</html>
